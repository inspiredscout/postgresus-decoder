Готовый **README.md** — просто вставляй как есть:

---

````markdown
# postgresus-decoder

Инструмент для расшифровки зашифрованных бэкапов Postgresus.  
Автоматически использует `master key` из `.env`, определяет `backup_id` по имени файла и выполняет потоковую дешифровку (AES-GCM) в полном соответствии с оригинальной реализацией.

---

## 🚀 Возможности

- Чтение master-key из `.env`
- Автоматическое определение `backup_id` по имени файла
- Дешифровка постраничных chunk'ов (AES-GCM)
- Поддержка оригинального формата Postgresus Backup
- Сохранение результата в файл `{backup_id}.dump`

---

## 📦 Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/inspiredscout/postgresus-decoder
cd postgresus-decoder
````

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Создайте файл `.env`:

```env
POSTGRESUS_MASTER_KEY=ваш_секретный_ключ
```

---

## 🧠 Как использовать

```bash
python script.py {backup_filename}
```

Где:

* `{backup_filename}` — имя файла бэкапа, например:

```
3f9c1e15-3a8b-4e1f-b532-bac1bd854e21.backup
```

UUID автоматически используется как `backup_id`.

### Пример

```bash
python script.py 7a3d5c25-8d7c-4e11-9b0d-0c83dfab2d81.backup
```

После выполнения появится файл:

```
7a3d5c25-8d7c-4e11-9b0d-0c83dfab2d81.decoded
```

---

## 📝 Требования

* Python 3.9+
* OpenSSL 1.1+
* Файл `.env` c переменной `POSTGRESUS_MASTER_KEY`

---


## ⚠️ Важно

* Никогда не храните master-key в открытом доступе.
* Ограничьте доступ к `.env`.
* Расшифрованные данные могут содержать чувствительную информацию — храните их безопасно.

---
